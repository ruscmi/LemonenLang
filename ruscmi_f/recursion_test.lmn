/-
    Test 1: Deep Recursion & Call Stack
-/
lmport(ansi);

func fib(n) {
    if (n <= 1) { return n }
    return fib(n - 1) + fib(n - 2)
};

lmuck("Calculating fib(30)...");
// Померяй время до и после
res = fib(30);
lmuck("Result:", res);
